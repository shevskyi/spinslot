# Gates of Olympus™ — Slot Machine Game

Повнофункціональна гра на слот-машині з грецькою тематикою Олімпу з динамічною анімацією, системою виграшів та кінематичними ефектами.

## 📋 Структура проєкту

```
Gates of Olympus™/
├── clean.html                 # Основний файл гри (單-файл програма)
├── spin-config.json          # Конфіг комбінацій символів
├── README.md                 # Документація
├── assets/
│   ├── animations/           # GIF анімації Зевса
│   ├── audio/                # Звукові ефекти
│   └── png/
│       ├── background/       # Фонові зображення
│       └── symbols/          # Символи слоту
└── js/
    └── sound.js              # Логіка відтворення звуків
```

## 🎮 Основні можливості

### 1. **Механіка гри**
- **6 барабанів × 6 символів** на екрані
- **Система виграшів** з 5 рівнями:
  - `small` — Малий виграш (10-60 кредитів)
  - `medium` — Середній виграш (75-225 кредитів)
  - `big` — Великий виграш (250-650 кредитів)
  - `mega` — МЕГА ВИГРАШ (1000-2500 кредитів)
  - `none` — Програш (мінус 2% від загального балансу)

### 2. **Логіка без повторень**
Система передбачає чергування результатів без послідовних повторень:
- **Small Win** → потім **Big Win** або **Try Again**
- **Big Win** → потім **Try Again** або **Small Win**
- **Try Again** → потім **Small Win** або **Big Win**
- **MEGA WIN** — гарантований кожні 5 спінів (на спінах 5, 10, 15, 20...)

### 3. **Кінематичні ефекти**
- **Fullscreen Lightning** — молнії на весь екран під час MEGA WIN
- **Zeus Lightning** — анімація молній від Зевса
- **Slot Area Lightning** — молнії в區зоні барабанів
- **Particle Effects** — вибухові частинки та конфеті
- **Screen Shake** — тремтіння екрану
- **Camera Zoom** — наближення камери під час великих виграшів

### 4. **Анімація Зевса**
- **Smooth Movement** — плавне 2D переміщення
- **Multiple Poses** — різні GIF анімації:
  - `zeus_idle.gif` — Стандартна позиція
  - `zeus_idle_thunder.gif` — Позиція з грозою
  - `zeus_summon_titan_symbol.gif` — Позиція виклику титана
  - `zeus_super_charging_wild.gif` — Позиція суперзаряду (MEGA WIN)
- **Hover Animation** — реакція на спін

### 5. **Звукове оформлення**
- Звук спіну (`spin-sound`)
- Звук виграшу (`win-sound`, `big-win-sound`)
- Звук MEGA WIN (`mega-win-sound`)
- Звук монет (`coin-sound`)
- Звук клику (`click-sound`)
- Звук вибуху (`ice-win-6`)

### 6. **UI компоненти**
- **Win Status** — відображення результату спіну
- **Total Win Amount** — загальний баланс гравця
- **Previous Win Block** — попередній виграш
- **Falling Multiplier** — анімація множника (x25-x50)
- **MEGA WIN Overlay** — великий текст MEGA WIN на весь екран

## 🛠️ Технічні параметри

### Константи гри
```javascript
TOTAL_SPIN_SYMBOLS = 36          // Всього символів в спін-последовательности
MEGA_WIN_INTERVAL = 5            // MEGA WIN кожні N спінів
LOSS_PERCENT = 2                 // Відсоток знімання при програші
smallWinPenaltyPercent = 20      // Штраф для малих виграшів
```

### Win Level по кількості позицій
```javascript
0 позицій     → 'none' (програш)
1-3 позиції   → 'small' (малий виграш)
4-6 позицій   → 'medium' (середній виграш)
7-9 позицій   → 'big' (великий виграш)
10+ позицій   → 'mega' (мега виграш)
```

### Z-Index Hierarchy
```
999999999  — Falling Multiplier (на самому верху)
9999       — Win Status Text
8          — MEGA WIN Overlay
9998       — Slot Lightning
9999       — Zeus Lightning
```

## 🎯 Основний цикл гри

```
1. Гравець натискає "SPIN"
   ↓
2. getNextOutcome() визначає результат (з логікою чергування)
   ↓
3. generateGridFromCombination() створює рід з вираженням
   ↓
4. spinReels() запускає анімацію барабанів
   ↓
5. calculateWinLevel() визначає рівень виграшу
   ↓
6. showWinningAnimations() запускає кінематику (залежно від рівня)
   ↓
7. updateTotalWin() додає виграш до балансу
   ↓
8. updateUIForWinLevel() оновлює інтерфейс
   ↓
9. showWinStatus() показує текст результату
```

## 🔄 Функції управління грою

### Основні функції
- `nextSpin()` — Запуск одного спіну з визначенням результату
- `spinReels(targetGrid, winPositions, winLevel)` — Анімація барабанів
- `showWinningAnimations(winPositions, winLevel)` — Кінематичні ефекти
- `getNextOutcome()` — Визначення результату з логікою чергування
- `calculateWinLevel(winPositions)` — Розрахунок рівня виграшу

### Ефекти
- `createZeusLightning()` — Молнії від Зевса
- `createFullScreenLightning()` — Молнії на весь екран
- `createSlotAreaLightning(winLevel)` — Молнії в區зоні барабанів
- `addSymbolParticleEffect(element, winLevel)` — Частинки на символах
- `createConfettiAt(targetElement, count)` — Конфеті ефект

### Audio управління
- `playSound(id)` — Відтворення звуку за ID
- `explosionOnlyMode` — Режим, де лише вибух гримить

## 📁 Assets

### Анімації (GIF)
```
assets/animations/
├── zeus_pose_all-zeus_idle.gif
├── zeus_pose_all-zeus_idle_thunder.gif
├── zeus_pose_all-zeus_summon_titan_symbol.gif
└── zeus_pose_all-zeus_super_charging_wild.gif
```

### Звуки (MP3/WAV)
```
assets/audio/
├── spin-sound.mp3
├── win-sound.mp3
├── big-win-sound.mp3
├── mega-win-sound.mp3
├── coin-sound.mp3
├── click-sound.mp3
└── ice-win-6.mp3
```

### Символи
```
assets/png/symbols/
├── symbol_1.png
├── symbol_2.png
├── ... (всі символи слоту)
└── background/
    └── main-bg.jpg (фоновое зображення)
```

## 🚀 Запуск

1. **Відкриття гри:**
   ```
   Просто відкриєте clean.html у браузері
   ```

2. **Конфігурація символів:**
   Відредагуйте `spin-config.json` для зміни комбінацій виграшів

3. **Заміна звуків:**
   Помістіть нові MP3/WAV файли в `assets/audio/` і оновіть ID в `js/sound.js`

4. **Заміна анімацій:**
   Помістіть нові GIF файли в `assets/animations/` та оновіть шляхи в `clean.html` (рядки з `zeus_pose_all-...`)

## 📝 Комбінації символів

Комбінації визначаються у `spin-config.json` та фільтруються по `winLevel`:
- Кожна комбінація має формат: `{ positions: [col, row, ...], winLevel: 'small|medium|big|mega' }`
- При спіні вибирається рандомна комбінація для відповідного рівня

## 🎓 Як працює логіка чергування

```javascript
lastOutcome = null  // Запам'ятовування останнього результату

Спін 1: null → рандом (small|big|try)
Спін 2: small → big або try (не small)
Спін 3: big → try або small (не big)
Спін 4: try → small або big (не try)
Спін 5: MEGA WIN (гарантований)
Спін 6: null (reset) → рандом (small|big|try)
...
```

## 🐛 Важливі глобальні змінні

```javascript
spinCount          // Лічильник спінів (для визначення MEGA WIN)
lastOutcome        // Останній результат (для чергування без повторів)
totalWinAmount     // Загальний баланс гравця
isSpinning         // Флаг чи зараз крутиться
```

## ✨ Особливості реалізації

- **Single-file архітектура** — весь код у clean.html
- **Без зовнішніх залежностей** — чистий JavaScript
- **Web Animations API** — для плавних ефектів
- **Canvas 2D** — для кінематичних ефектів
- **SVG** — для молній
- **CSS Transforms** — для анімацій барабанів (Y-axis flip)

## 🔧 Налаштування

Для зміни основних параметрів редагуйте константи на початку `clean.html`:

```javascript
MEGA_WIN_INTERVAL = 5        // Змініть на бажану кількість спінів до MEGA
LOSS_PERCENT = 2             // Змініть штраф при програші
smallWinPenaltyPercent = 20  // Змініть штраф для малих виграшів
```

## 📞 Підтримка

Гра повністю локалізована під українську мову та адаптована під мобільні пристрої (9:16 aspect ratio).
